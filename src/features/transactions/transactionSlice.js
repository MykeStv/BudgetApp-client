import { createSlice } from '@reduxjs/toolkit'

//Slice contains the data of the feature state
export const transactionSlice = createSlice({
    name: 'transaction',
    initialState: [],
    reducers: {
        
    }
})

export default transactionSlice.reducer